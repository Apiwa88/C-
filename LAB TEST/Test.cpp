#include <stdio.h>
int main()
{
    int num;
    cout << "Enter you number :";
    cin >> num;
    int sum = 0;
    int cout = 0;
    for (int j = 1; i <= n; i++)
    {
        for (int j = 1; j <= n; i++)
        {
            if ((j % 1 == 0) && (j % i == 0))
            {
                count = count + 1;
            }
        }
        if (count == 2)
        {
            sum = sum + i;
        }
    }
    cout << sum << ;
    return (0);
}
